""" 

Common list functions
    - list[-1] - reverse retrieval starts at -1

Adding to list
    - .append()
    - .extend()
    - .insert() - insert at specific index but if with -1, never last index but rather index before last 

Removing from list
    - .clear() - clear list completely
    - .pop() - remove last index if not specifed or specific index if specified
    - .remove() - remove first value in list e.g [1,3,3,7].remove(3) will remove only index 1

"""